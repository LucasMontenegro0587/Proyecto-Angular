import { TamanioLetraDirective } from './tamanio-letra.directive';

describe('TamanioLetraDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanioLetraDirective();
    expect(directive).toBeTruthy();
  });
});
